const toggleBtn = document.getElementById('togglePortfolioBtn');
const wrapper = document.getElementById('projectWrapper');

toggleBtn.addEventListener('click', () => {
  // ลองเขียนเอง: สลับ class 'is-open' แล้วเปลี่ยนข้อความปุ่มตามสถานะ
});